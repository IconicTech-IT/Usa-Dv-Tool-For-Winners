# سجل البناء — الجلسة السحابية

> الملف ده بيشرح **إيه اللي اتبنى وليه**، عشان أي جلسة جديدة تفهم القرارات
> اللي مش باينة من الكود لوحده. مش تكرار لـ`BRIEF.md` (المواصفات) ولا
> `WHAT-I-NEED-FROM-YOU.md` (المطلوب من صاحب المشروع).
>
> **الجلسة دي كانت شغالة على السحابة**، وشبكتها كانت بتمنع أي دومين خارجي.
> عشان كده عدد الحقول `verified` كان **صفر** لما خلصت — مقصود مش إهمال.

---

## الحالة لما الجلسة خلصت

| | |
|---|---|
| Commits | ٨ (`00ab91e` → `2bbe953`) |
| صفحات | ٣٥ route، كلها static، بالعربي والإنجليزي |
| Tests | ٨٩ |
| a11y | صفر مخالفات WCAG 2.1 AA على ١٤٠ نسخة صفحة (٣٥ × لغتين × ثيمين) |
| أكبر route | ١٦٢KB (السقف ٢٠٠) |
| ملفات محتوى | ٩٩ |

---

## القرار المعماري الأهم: نظام الـ٤ حالات

الستارتر كان فيه ٣ حالات، والداتا كانت بتكسر الschema بتاعها (٤٣٦ حقل
`estimated` من غير مصدر). القرار كان **نرخّي القاعدة مش نشدّها**، لأن لو كل
تقدير معقول بقى "محتاج تأكيد" الموقع كله هيبقى بادچات صفرا والبادج يفقد معناه.

| الحالة | الشرط | في الواجهة |
|---|---|---|
| `verified` | قيمة + مصدر رسمي | من غير بادج |
| `estimated` | قيمة + `basis` + `verifyIn`. المصدر اختياري | بادج "تقديري"، الbasis في tooltip |
| `judgment` | قيمة + `label`. **ممنوع** مصدر | من غير بادج، بيتعرض كوصف مش رقم |
| `NEEDS_VERIFICATION` | `value: null` | بادج "محتاج تأكيد" |

**⚠️ `judgment` أخطر حالة.** `arabCommunity` و `gigDemand` أحكام شخصية مش
قياسات — مفيش مصدر بيرتب المدن رقميًا عليهم. `display()` في
`lib/content/field.ts` **مبترجعش رقم للحالة دي أصلًا**، والرقم بيخرج بس من
`rankValue()` للمحرك. القاعدة متشفّرة مش مكتوبة.

**فرق `note` و `basis` — مهم:** `note` للمراجع البشري، `basis` بيظهر
للمستخدم في الtooltip. أي تحذير المفروض المستخدم يشوفه لازم يكون في `basis`.

---

## قاعدة الترتيب: الحقايق بترتب، الأحكام بتفصل بين المتعادلين

`lib/planner/judgment.ts`

أول تنفيذ كان **سقف وزن** (١٥٪). الtest كسره: مدينة حقايقها أقل بـ١٢٪ لسه
كانت بتقدر تتصدّر، لأن ١٢ أقل من ١٥. **السقف لوحده مش بيمنع قلب الترشيح.**

الحل النهائي مرحلتين:
1. رتّب بالحقايق
2. المدن اللي فرقها أقل من `JUDGMENT_TIE_BAND` (٥٪) تعتبر متعادلة، والأحكام
   الذاتية بترتب **جوه المجموعة المتعادلة بس**

كده الحكم الذاتي ميقدرش ينقل مدينة من تحت لفوق مهما كان رقمه — بالبناء مش
بالسقف. الحالة اللي كسرت السقف متسجلة كtest.

---

## ⚠️ أخطر ٤ باجات اتكشفت — وكلها من نوع واحد

كلهم كانوا **بيشتغلوا والأرقام بتطلع**، بس غلط بشكل بيتقرا كإنه صح.
والـbuild كان أخضر في كل مرة. اللي كشفهم هو **فتح الصفحة والبص**.

### ١. الخطة كانت بتكذب بالداتا الفاضية

بأرقام ناقصة، البنود بتتشال من الجمع → المصاريف تطلع **$150 في الشهر** →
الموقع بيقول لواحد معاه **$6,000** إن فلوسه تكفيه **٣٠ شهر**.

**الحل:** `computable` + `missingEssential` في `PlanResult`، والواجهة
**بترفض تعرض أي رقم** من غير إيجار وأكل المدينة.

### ٢. مدن مجهولة كانت بتتقدّم على مدن معروفة

نفس الباج مستوى أعلى: مدينة داتاها فاضية مصاريفها بتتحسب أقل من الحقيقة،
فبتتصدّر الترشيح وتزق المدينة اللي عندها أرقام حقيقية لقايمة "اتجنبها".

**الحل:** `MetroScore.computable`، والمدن المجهولة بتتشال من الترشيح خالص.
ولو مفيش ولا مدينة معروفة، القايمة بتفضل فاضية وبتقول ليه — مش بتكذب.

### ٣. صفحتين شيك ليست كانوا واقعين تمامًا

`useUser((s) => s.checklists[id] ?? {})` بيعمل object جديد كل render، وzustand
بيقارن بالمرجع → render تاني → object جديد → **حلقة لا نهائية** (React #185).
الصفحتين كانوا بيقعوا من غير ما حد ياخد باله.

**الحل:** مرجع ثابت `NO_CHECKS` خارج الcomponent.

### ٤. مخالفة قواعد الـhooks عدّت من TypeScript

`early return` قبل `useEffect` في `BalanceChart`. TypeScript مبيمسكش ده.
**عشان كده اتضاف ESLint** بقواعد الhooks — ومسك كمان `kidsAges` بتتعمل من
الأول كل render فبتخلي `useMemo` يعيد الحساب من غير داعي.

---

## فيتشر: المستخدم يعدّل الأرقام بنفسه

`lib/planner/overrides.ts` · `components/planner/CostEditor.tsx`

**المبدأ: رقم المستخدم أصدق من أي تقدير عندنا.** إحنا بنقول متوسط مدينة؛ هو
لقى غرفة بعينها بسعر معيّن.

ترتيب القراءة: **رقم المستخدم ← "مش محتاجه" ← رقم الموقع ← ناقص**

و**"مش محتاجه" إجابة مش نقص بيانات** — المهاجر الجديد بيستغنى عن حاجات كتير
أول فترة (ساكن عند قريب، مش هيشتري عربية، الفواتير داخلة في إيجار الغرفة).
عشان كده بتفك الحسبة زي الرقم بالظبط.

**وده فك أهم قفلة:** كارت "مش قادرين نحسب" كان طريق مسدود، دلوقتي تحته
المحرر مباشرة.

**تلات باجات اتكشفت وأنا بجرب الفيتشر ده في المتصفح:**
- التعديل كان بيضيع: يعدّل إيجار مدينة → الترتيب يتغير → الخطة تختار مدينة
  تانية إيجارها ناقص → دايرة. الحل: المدينة اللي عدّلها بتفضل هي المختارة
- الباج رقم ٢ فوق (المدن المجهولة)
- الخطة كانت بتتقفل على مدينة مجهولة رغم وجود مدينة معروفة متاحة

---

## قرارات خالفت البريف — محتاجة مراجعة صاحب المشروع

### ١. مستخدمتش GSAP ولا framer-motion
البريف طالبهم. الاتنين مع بعض **+٩٠KB** على ميزانية ٢٠٠KB لكل route، وكل
حركة مطلوبة اتنفذت من غيرهم بـanime.js + CSS + IntersectionObserver — بما
فيها رسم خط الرصيد لحد ما يلمس الصفر.

### ٢. الخطوط من Google Fonts
البريف كان طالب خطوط محلية. صاحب المشروع اختار جوجل. اتنفذت بـ
`next/font/google` اللي **بينزّل الملفات وقت الـbuild ويقدّمها من نفس
الدومين** — فمفيش طلب وقت التشغيل ولا تتبع، وأقرب حاجة لشرط البريف.

### ٣. Next 15 مش 16
البريف قايل ١٥. ١٦ هو الحالي.

### ٤. service worker مكتوب بالإيد
`next-pwa` توافقه مع Next 15 مش مستقر. `public/sw.js` ٦٠ سطر.

---

## ⚠️ دين تقني معروف: أرقام مخترعة جوه الكود

`lib/planner/engine.ts` فيه:

```
TRAVEL_COST_PER_ADULT = 900 · TRAVEL_COST_PER_KID = 700
SETUP_PER_HOUSEHOLD = 600 · SETUP_PER_EXTRA_PERSON = 150 · phone = 30
```

**دي مخالفة صريحة للقاعدة الأولى في `CLAUDE.md`** — وبتظهر للمستخدم "تذاكر
السفر $900" من غير بادج و`incomplete: false`. يعني رقم مخترع معروض كحقيقة.
و`fees.json` نفسه فيه بند بيقول إن الطيران لازم يتاخد كمُدخل من المستخدم.

**التوصية:** انقلهم `content/` بحالة `estimated` مع `basis` يقول الافتراض
(من مصر، اقتصادي، موسم عادي). **متفضّهمش** — `CostEditor` موجود خلاص
والمستخدم يعرف تذكرته أحسن مننا. الرقم هيفضل نفسه، اللي هيتغير إنه بقى
معلّم تقديري وقابل للتعديل.

---

## حاجات مبنية ومستنية توصيل

- **`fees.json` مش معروض في أي صفحة.** `loadFees()` بيتنادى من
  `verify-content` بس. مكانه الطبيعي `/timeline`
- **`content/tax-brackets.json`** اتعمل في الجلسة دي وكل قيمه كانت `null`

---

## ملاحظة على المنهج

الأربع باجات فوق كلهم من نوع واحد: **الكود شغال، الأرقام بتطلع، الـbuild
أخضر — والنتيجة غلط بشكل بيتقرا كإنه صح.**

الـtests مسكتهم **بعد** ما اتفتحت الصفحة واتشافت بالعين. لو الجلسة وقفت عند
"الـbuild أخضر و٨٩ test عدّوا"، الموقع كان هينزل وهو بيقول لناس بتخطط بفلوسها
إن $6,000 تكفيهم ٣٠ شهر.

**الـbuild الأخضر مش دليل. البص على الصفحة هو الدليل.**
