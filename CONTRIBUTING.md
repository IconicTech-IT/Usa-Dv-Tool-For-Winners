# المساهمة

أهلًا. أي تصحيح لمعلومة غلط هو أنفع مساهمة ممكنة هنا.

## لقيت معلومة غلط؟
افتح Issue فيها تلات حاجات بس:
1. المعلومة الغلط (وفي أنهي صفحة)
2. المعلومة الصح
3. **لينك المصدر الرسمي** — ده أهم نقطة، من غيره مش هينفع نغيّر

## عايز تعدّل بنفسك؟
1. Fork
2. عدّل الملف في `content/` (اقرا الجدول في `README.md`)
3. غيّر `value` و `status` و `lastVerified` مع بعض
4. `npm run verify-content` عشان تتأكد إن مفيش حاجة اتكسرت
5. Pull Request فيه لينك المصدر

## قواعد المحتوى
- **مصادر رسمية بس** للأرقام والإجراءات: travel.state.gov · uscis.gov · ssa.gov · irs.gov · healthcare.gov · مواقع الولايات
- تجربة شخصية ممكن تتضاف، بس متتكتبش كـ"معلومة" — تتكتب كـ"تجربة" وواضح إنها كده
- مش متأكد من رقم؟ `null` + `NEEDS_VERIFICATION`. مفيش عيب في كده، العيب في الرقم المغلوط
- ممنوع نصايح قانونية · ممنوع أي محتوى بيعلم حد يجمّل إجابة في مقابلة السفارة
- ممنوع أي حاجة بتكسب فلوس: إعلانات، affiliate، رعاية. الموقع مجاني وهيفضل كده

## الكود
- logical CSS properties بس (مفيش `left`/`right`)
- كل نص جديد يتحط في `messages/ar.json` و `messages/en.json`
- أي حساب جديد يتكتبله unit test
