# تسليم — جلسة ٢٦ أغسطس ٢٠٢٦

> الجلسة اللي قبلها سابت `WHAT-I-NEED-FROM-YOU.md` وفيه تلات طلبات. الملف ده
> بيقول اتعمل فيهم إيه، وإيه اللي اتكشف وإحنا بنعملهم، وإيه اللي لسه فاضل.
>
> **١٢ commit · كلهم متبعوتين على `claude/repo-purpose-discussion-bc9lxb`.**
> والموقع شغال على <https://dv-compass.vercel.app/ar>.

---

## الحالة دلوقتي

```
✅ المحتوى سليم — 1302 حقل
   verified 18 · estimated 512 · judgment 130 · محتاج تأكيد 642
✅ 104 test  ·  build أخضر (85 صفحة)  ·  tsc و lint نضاف
```

**الحقول الفاضية نزلت من ٨٤٢ لـ٦٤٢.** والخطة بقت تشتغل في **الخمسين مدينة**،
للفرد الرايح لوحده وللعيلة الداخلة مع بعض — قبل كده كانت شغالة في ٨ مدن
وللفرد بس.

---

## الطلبات التلاتة

### ١. الأرقام الرسمية ✅

الجلسة السابقة قالت إن الشبكة عندها بتمنع `irs.gov` و`travel.state.gov`
و`uscis.gov`. **عندنا هنا مفتوحين**، فاتملّوا.

**`content/tax-brackets.json` — ١٥ حقل `verified`، سنة ٢٠٢٦:**

المصدر مش صفحة الأخبار — **Rev. Proc. 2025-32** نفسه في نشرة IRS الرسمية
([IRB 2025-45](https://www.irs.gov/irb/2025-45_IRB)). وده كان فارق: صفحة
الأخبار بتاعة IRS بتدي حدود "عائل الأسرة" غلط لأنها بتنسخها من الأعزب.
الـRev. Proc. بيقول ‎17,700/67,450/201,750/256,200‎ مش
‎12,400/50,400/201,775/256,225‎.

| | |
|---|---|
| الخصم القياسي | أعزب 16,100 · متجوز 32,200 · عائل أسرة 24,150 |
| الشرايح | ٧ شرايح كاملة للتلات حالات |
| ضمان اجتماعي | ٦.٢٪ بسقف **184,500** |
| ميديكير · إعفاء الطفل | ١.٤٥٪ · 2,200 |
| العمل الحر · خط الفقر | ١٥.٣٪ · 15,960 + 5,680 للفرد |
| نسبة الميل | **0.76** |

> ⚠️ **نسبة الميل اتغيّرت في نص السنة:** ٧٢.٥ سنت من يناير لـ٣٠ يونيو،
> و٧٦ سنت من ١ يوليو. اللي في الملف هو الساري، والتحذير مكتوب في الـ`note`.

**`content/fees.json` — تلات رسوم `verified`:**

- رسم التسجيل **$1** — وكان `perPerson: true` وده **غلط**. الصفحة الرسمية
  بتقول بالنص *"paid at time of registration only by the principal applicant"*.
  عيلة من ٤ بتدفع دولار واحد مش أربعة.
- رسم فيزا القرعة **$330** لكل فرد
- رسم USCIS **$235** من **G-1055 نسخة 05/29/26** — مش من صفحة "our fees"
  اللي طلعت **أرشيف بأسعار ٢٠١٦** وفيها الرقم 220
- الفحص الطبي والفيش والترجمة والطيران فضلوا `NEEDS_VERIFICATION` — بيختلفوا
  من بلد لبلد فمفيش رقم واحد صح ليهم

### ٢. أرقام المدن ✅ — واتوسّعت للخمسين

الطلب كان `roomRent` و`groceriesPerAdult` لتمن مدن. اتعملوا، وبعدين اتوسّعوا
للخمسين مدينة وللشقق كمان لما بان إن العيلة الداخلة مع بعض كانت لسه متوقفة.

| | التغطية | المصدر |
|---|---|---|
| `roomRent` | ٣٤ مدينة | وسيط إعلانات فعلية على SpareRoom (٣٢–٢٠٤ إعلان للمدينة) + الربع الأدنى والأعلى |
| `roomRent` | ١٦ مدينة | **متوقّع** من معادلة، والbasis بيقول كده صراحةً |
| `apt1br` · `apt2br` | ٥٠ مدينة | HUD FY2026 الرسمي |
| `groceriesPerAdult` | ٥٠ مدينة | خطة USDA منخفضة التكلفة، يونيو ٢٠٢٦ |

### ٣. Vercel ✅ — الموقع شغال، وأنا كنت غلطان

**الموقع منشور وشغال:** <https://dv-compass.vercel.app/ar> بيرجّع `200`،
والمشروع على <https://vercel.com/eslam-nassef/dv-compass>.

> ⚠️ **الغلطة دي تستاهل تتقرا كويس.** أنا قلت قبل كده "مفيش ولا مشروع على
> الحساب خالص" لأن `list_projects` رجّعت `[]`. **القايمة الفاضية مكانتش
> معناها مفيش مشروع — كانت معناها إن التوكن بتاعي مش شايفه.** والدليل إن
> `list_deployments` على نفس المشروع بترجّع **403 Forbidden** (مش 404):
> يعني المورد موجود والصلاحية ناقصة.
>
> ومراجعة من شات تاني كانت قالت الكلام ده بالظبط — إن فيه build log فعلي
> فلازم يكون فيه مشروع مربوط. **كانت على حق وأنا رفضت الاستنتاج بناءً على
> أداة معماها محدود.**
>
> **الدرس:** أداة بترجّع فاضي ≠ الحاجة مش موجودة. اتأكد من نوع الخطأ
> (403 مقابل 404) قبل ما تبني عليه استنتاج.

**والنشر متابع الريبو فعلًا** — الموقع الحي بيعرض `مؤكد 18 · تقديري 512`،
وهي نفس أرقام آخر commit. يعني الـauto-deploy شغال من الفرع الرئيسي.

إصلاح الـpeer dependency (`fd5c530`) موجود ومتبعوت.

---

## الباجات اللي اتكشفت

كلها من نفس النوع اللي `WHAT-I-NEED-FROM-YOU.md` حذّر منه: **الكود شغال
والرقم بيطلع، بس غلط بشكل بيتقرا كإنه صح.** ومحدش منهم اتمسك من الـbuild
ولا من الـtests — كلهم اتمسكوا من فتح الصفحة والبص.

### 🔴 تأمين السكن كان بيطلع $1,684,970

`securityDeposit` متخزن كـ**مضاعف للإيجار** (١ = إيجار شهر)، بس اللي بيتعرض
دولارات والمحرر بياخد دولارات كمان. واحد كتب `1400` (يقصد ‎$1,400‎) على إيجار
‎$1,200‎ فاتضربوا. **الرقم المعروض ودخل المحرر كانوا بوحدتين مختلفتين.**

الحل: رقمنا مضاعف يتضرب، ورقم المستخدم دولار زي ما هو. وتلات tests بيمسكوا
الحالة، واحد منهم بيقول إجمالي الوصول لازم يفضل تحت ‎$20,000‎.

### 🔴 `tax-brackets.json` مكانش في `collectFields()`

يعني الـ١٧ حقل الضريبي الفاضي **عمرهم ما ظهروا في `TODO-VERIFY.md`**. التقرير
كان بيقرا كإنه كامل وهو مش شايف أهم ملف في الموقع. قايمة الملفات اتنقلت
لـ`contentFiles()` وعليها تحذير إن أي ملف جديد لازم يتحط فيها.

### 🔴 أرقام مخترعة مكتوبة في `engine.ts`

`900` طيران · `700` طفل · `600` تأسيس · `150` فرد إضافي · `30` تليفون ·
و`120` بنزين **مدفونة جوه سطر تأمين العربية**. كانت بتوصل للمستخدم كـ"تذاكر
السفر $900" **من غير بادج**، ومخالفة للقاعدة الأولى في `CLAUDE.md` نصًا.
اتنقلوا لـ`content/arrival-costs.json` بحالة `estimated` وbasis بيقول الافتراض.
القيم زي ما هي — اللي اتغيّر إنها بقت معلّمة وقابلة للتعديل.

### 🔴 الفواتير كانت هتتحسب مرتين

إيجار HUD شامل المياه والكهربا، والمحرك بيضيف `utilities` كبند منفصل.
النهارده مفيش مشكلة لأنها `null` — بس ملاحظة في JSON مش بتوقف حد بعد ٦ شهور.
**اتشفّر في تلات طبقات:**

1. علامة `includesUtilities` على بند السكن
2. المحرك بيتخطى البند ويقول السبب بدل "مش عارفينه"
3. الـschema **بيفشل الbuild** لو حد ملا `utilities` لمدينة إيجارها شامل

وأول ما المستخدم يحط إيجار من إعلان فعلي، البند بيرجع — لأن الإعلانات غالبًا
من غير فواتير.

### 🟡 صفحة `/sources` كانت بتكدب

قايمة مصادر مكتوبة بالإيد فيها `ssa.gov` و`healthcare.gov` **والمحتوى مش
بيستشهد بيهم خالص**، ومفيهاش المصادر اللي بيستشهد بيها فعلًا. بقت بتتولد من
المحتوى نفسه بـ`collectSourceHosts()`.

### 🟡 `fees.json` محدش كان بيعرضه

`loadFees()` كان بيتنادى من سكريبت التحقق **بس**. رقم مؤكد مخبّي في ملف قيمته
صفر للمستخدم. بقى معروض في `/timeline` بـ`components/tools/Fees.tsx`.

### 🟡 رفرفة الكارت عند الحواف

الميل بيحرّك حدود الكارت، وده كان بيولّد تلات مشاكل مع بعض:

1. **خروج وهمي** — الميل بيزيح السطح من تحت الماوس فالمتصفح يبعت
   `pointerleave` والماوس ماتحركش → الرجوع يشتغل → الكارت يرجع تحت الماوس →
   ييل تاني. **حلقة لا نهائية.**
2. `outElastic` بيعدّي مكانه ويرجع، فالحافة بتمسح على الماوس كذا مرة.
3. `getBoundingClientRect` بترجع المستطيل **بعد** الميل، فالحساب كان بيتغذى
   على نتيجته — تذبذب أوضح في الأركان لأن المحورين بيعملوا كده مع بعض.

الحل: كل القياسات على المستطيل **المسطّح** (`restRectOf` بيلغي الـtransform
للحظة القياس)، و`pointerleave` والماوس لسه جوه = خروج من صنعنا فبيتتجاهل.

### 🟡 كل عناصر الإدخال كانت `bg-transparent`

الشكل المقفول كان ماشي، بس **قايمة الـ`<select>` لما بتفتح** المتصفح بيرسمها
بخلفية النظام وبيسيب لون النص — فالنص الفاتح بتاع الدارك كان بيقع على أبيض.
توكن `--field-bg` صريح + قاعدة لـ`option`. اتقاس في المتصفح: **١٣.٥:١ دارك ·
١٥.٨:١ لايت**.

### 🔴 سطر العربية كان بيتجاهل صفر المستخدم

سطر "تأمين العربية والبنزين" بيعرض حاجتين، والمحرر بياخد رقم واحد
(`carInsurance`) والبنزين كان بيتضاف فوقه دايمًا. **المستخدم كتب ٠ وشاف ١٢٠.**
نفس غلطة التأمين بالحرف: اللي بيتعرض واللي المحرر بياخده لازم يكونوا نفس
الحاجة. دلوقتي رقم المستخدم (أو "مش هحتاجه") هو **السطر كله**، والبنزين
بيتضاف على رقمنا إحنا بس. ٤ tests بيغطوا الحالات الأربعة.

### 🟡 قايمة الأرقام الناقصة كانت بتقول `austin-tx.monthlyTransitPass`

اسم برمجي المستخدم بيقراه ومبيفهمش منه حاجة — وده بالظبط المكان اللي
المفروض يفهم منه إيه اللي ناقص عشان يقرر يعتمد على الحسبة ولا لأ.
`lib/content/labels.ts` فيه أسماء كل الحقول بالعربي والإنجليزي، والقايمة
بقت تقول **"اشتراك المواصلات الشهري — أوستن"**. أي حقل جديد ممكن يظهر
كناقص لازم يتحط هناك.

### 🟡 تنسيق أرقام غير متسق

`CountUp` كان بيطلع `$5974` جنب تفصيل مكتوب فيه `$3,468` — نفس الصفحة ونفس
نوع الرقم بشكلين. وفي `Badge.tsx` كان `basis?.ar` ثابت، يعني اللي بيتصفح
بالإنجليزي بيلاقي شرح التقدير بالعربي.

---

## غلطتين في شغلي أنا اتصلحوا

مهم تعرفهم لأن الاتنين من نفس النوع اللي بنحاربه.

### الطريقة الأولى للأوض كانت منحازة لتحت

بدأت بـ`HUD_2BR ÷ 3`. لما جمّعت إعلانات فعلية وقارنت في السبع مدن اللي كانت
متملية، **السبعة كلهم كانوا أقل من السوق**:

| المدينة | القديم | السوق | الفرق |
|---|---|---|---|
| كولومبوس | 570 | 900 | **+58%** |
| شيكاغو | 760 | 1,095 | **+44%** |
| ديربورن | 570 | 800 | **+40%** |
| فيلادلفيا | 720 | 950 | +32% |
| نيويورك | 1,210 | 1,590 | +31% |
| شارلوت | 690 | 795 | +15% |
| هيوستن | 710 | 800 | +13% |

يعني الخطة كانت بتقول للناس إن فلوسهم تكفي **أطول** من الحقيقة — وده الاتجاه
الخطر.

### والنسبة الثابتة للمدن الناقصة كانت غلط كمان

للمدن اللي مفيهاش إعلانات استخدمت نسبة واحدة (`× 0.525`). **اللي كشفها سؤال
من صاحب الموقع عن نيويورك:** سان فرانسيسكو طلعت ‎$1,890‎ — يعني أغلى من
نيويورك المقيسة ‎$1,590‎، وده مستحيل.

السبب: النسبة **مش ثابتة**، بتقل كل ما المدينة تغلى (ارتباط ‎−0.59‎) — أغلى ٨
مدن ‎0.46‎ وأرخص ٨ ‎0.64‎. سعر الأوضة سقفه اللي **فرد واحد** يقدر يدفعه، بينما
إيجار الشقة بيفضل طالع.

البديل انحدار خطي متعلّم من نفس الـ٣٤ مدينة. **تحقق leave-one-out:**

| | خطأ متوسط | أسوأ حالة |
|---|---|---|
| نسبة ثابتة | ١٥.٢٪ | ٣٨.٩٪ |
| **انحدار خطي** | **١١.٦٪** | **٢٧.٢٪** |

وسان فرانسيسكو بقت ‎$1,470‎ — تحت نيويورك زي ما المفروض.

---

## مصدر اترمى بالكامل

SpareRoom مالوش صفحة لكل مدينة، فجربت endpoint البحث العام
`/flatshare/?search=`. رجّع نتايج — بس لما بصيت لقيت **الـ١٦ مدينة كلهم بنفس
الأرقام بالحرف**: `n:66 · وسيط 1200 · 19 من غير فواتير · 47 بفواتير`.
الendpoint بيتجاهل اسم المدينة ويرجّع آخر بحث.

لو اتوثق فيه كان هيتكتب **$1,200 مزيّف** في لوس أنجلوس وسان فرانسيسكو وأوماها
وهارتفورد — كلهم نفس الرقم. **اترمى كله**، والباقي اتجمّع من الصفحات المخصصة
اللي عنوانها بيأكد المدينة.

> **الدرس:** أي مصدر جديد، قارن نتيجته بين مدينتين مختلفتين قبل ما تصدقه.

---

## حاجة جديدة: ملف أسعار تتكتب بالإيد

**`data/الأسعار.csv`** — ٥٠ مدينة، صف لكل واحدة، يتفتح بإكسل.

```bash
npm run prices              # يكتب الملف في content/metros/
npm run prices -- --check   # يتحقق بس من غير ما يكتب
```

**كل سعر تلات خانات: `من` · `الأرجح` · `لـ`.** الرقم الواحد بيوحي بدقة مش
موجودة — الأوضة في نيويورك وسيطها ‎$1,580‎ بس نص الإعلانات بين ‎$1,325‎
و‎$1,955‎.

- `الأرجح` هو اللي الخطة بتحسب بيه. فاضي = "محتاج تأكيد"، مش صفر.
- `من`/`لـ` بيظهروا للمستخدم كـ**"أغلب الأسعار بين كذا وكذا"**.

**النطاقات مقيسة مش مخترعة:** التوزيع الكامل للـ٣٤ مدينة، والربع الأدنى
والأعلى الحقيقيين. والـ١٦ المتوقّعة نطاقها من نسبة الربعين المقيسة
(‎×0.842‎ و‎×1.191‎).

**والشقق من غير نطاق بقصد** — عندنا توزيع حقيقي للأوض مش للشقق مقسّمة بعدد
الغرف. اختراع نطاق ليها = نفس الغلطة اللي الموقع بيحاربها. الخانات فاضية عشان
تتملي من بحث حقيقي، ورقم Zillow جنبها كمرجع.

السكريبت بيتحقق قبل ما يكتب: `من` أكبر من `لـ` · رقم من غير مصدر · فواتير
مكتوبة مرتين · اسم مدينة غلط. **بيوقف ومبيكتبش حاجة.**

---

## اللي لسه فاضل

| الأولوية | الحاجة |
|---|---|
| 🔴 | **الأرقام المخترعة اللي في `arrival-costs.json`** — بقت معلّمة `estimated` وبasis، بس لسه افتراضات مش قياسات. الطيران بالذات المستخدم أعرف بيه |
| 🟡 | **`content/eligibility.json`** — كله فاضي، وأهم ملف يتحدّث كل دورة لأن قايمة الدول بتتغير |
| 🟡 | **`carNeed`** — ٥٠ مدينة `judgment`. الفرق بين ٣ و٤ في بالتيمور ومينيابوليس وبورتلاند وسياتل = آلاف الدولارات |
| 🟡 | **`carInsurance` و`monthlyTransitPass`** — فاضيين في الخمسين، وأعمدتهم موجودة في `data/الأسعار.csv` جاهزة |
| 🟡 | **الـ١٦ مدينة المتوقّعة** — استبدل أرقامها بإعلانات فعلية أول ما تلاقي مصدر |
| 🟡 | **أسماء الحقول** — `lib/content/labels.ts` لازم يتحدّث مع أي حقل جديد ممكن يظهر كناقص، وإلا المستخدم هيشوف المفتاح البرمجي |
| 🟢 | ٧٣ نص إنجليزي · ٧ نصوص `howItPays` لسه ماتكتبتش |
| 🟢 | `tsconfig.tsbuildinfo` متسجل في git وهو ملف بيتولد — يتحط في `.gitignore` |

---

## قواعد ثبتت نفسها في الجلسة دي

1. **الـbuild الأخضر مش دليل.** كل باج خطير هنا عدّى من الـbuild والـtests.
   اللي مسكهم هو فتح الصفحة والبص على الرقم.
2. **الرقم المعروض ودخل المحرر لازم يكونوا بنفس الوحدة.** باج الـ$1.68 مليون
   كان كله في السطر ده.
3. **أي تقرير بيتولد تلقائيًا، اتأكد إنه شايف كل الملفات** قبل ما تصدقه.
4. **أي مصدر جديد، قارنه بين مدينتين** قبل ما تكتب منه حاجة.
5. **متخترعش نطاق زي ما متخترعش رقم.** لو معندكش توزيع، سيب الخانة فاضية.
6. **أداة بترجّع فاضي ≠ الحاجة مش موجودة.** فرّق بين 403 و404 قبل ما تستنتج.
7. **أي سكريبت بيكتب في `content/` لازم يحافظ على `sources`.** أول نسخة من
   `prices.ts` كانت بتحط المصدر في `basis` بس وتسيب `sources: []` — فصفحة
   `/sources` قلّت مصدرين من غير ما حد ياخد باله. دلوقتي عمود المصدر بيقبل
   اللينك جواه والسكريبت بيفصله.

---

## أوامر

```bash
npm run dev              # شغّل محليًا
npm run prices           # اكتب أسعار data/الأسعار.csv في content/metros/
npm run verify-content   # تحقق + أعد كتابة TODO-VERIFY.md
npm run test             # 100 test
npm run build            # بيفشل لو أي رقم ناقصه مصدر أو basis
```

**اقرا `data/README.md`** — شرح ملف الأسعار بلغة بسيطة.
**والداتا الخام** وطريقة اشتقاق كل رقم في `data/rent-data-2026-08-26.json`.
