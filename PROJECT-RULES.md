# قواعد المشروع

> الملف ده بيتقرا تلقائي في كل session. `BRIEF.md` هو المواصفات الكاملة — ارجعله قبل أي مرحلة جديدة.
> و`CALCULATOR-SPECS.md` فيه مواصفات الحاسبات المالية — **اقراه قبل ما تبني أي حاسبة**.

## المشروع
موقع مجاني بيساعد أي حد بيفكر في قرعة الهجرة العشوائية لأمريكا (DV Lottery) — **مش الفايزين بس**. عربي مصري + إنجليزي. Next.js على Vercel.

**⚠️ الموقع شغال بالكامل لواحد مكسبش ومش مقدّم.** مفيش خانة رقم حالة، مفيش سؤال عن مرحلتك، مفيش ميزة مقفولة لحد ما يثبت إنه فايز. متابعة الإجراءات والتواريخ **اختيارية** بيفعّلها هو. أي feature بتفترض إن المستخدم فايز = غلط في التصميم.

## ٥ قواعد ملهاش استثناء

1. **متخترعش رقم.** أي رسم/إيجار/مدة مش متأكد منه من مصدر رسمي → `value: null`, `status: "NEEDS_VERIFICATION"`. رقم مغلوط أسوأ من مفيش رقم.
2. **كل معلومة عليها `lastVerified`، وكل رقم مؤكد عليه `sources`.** مصادر رسمية بس: travel.state.gov · uscis.gov · ssa.gov · irs.gov · healthcare.gov · موقع الولاية.
3. **مش استشارة قانونية.** وفي محاكي المقابلة: علّم الناس تجاوب **بصدق**. ممنوع أي محتوى بيعلم حد يجمّل إجابة.
4. **حدّث `TODO-VERIFY.md`** كل ما تضيف حقل محتاج تأكيد.
5. **مجاني للأبد.** مفيش login · إعلانات · affiliate · جمع إيميلات · زرار تبرع · ميزة مقفولة. ومفيش أي خدمة ليها فاتورة — تكلفة التشغيل لازم تفضل صفر.

## ٤ حالات للداتا و٣ بادچات

كل حقل في `content/` حالته واحدة من دول. الschema في `content/_schema.ts` بيفرضها والـbuild بيفشل لو اتكسرت.

| الحالة | الشرط | في الواجهة |
|---|---|---|
| `verified` | قيمة + مصدر رسمي | **من غير بادج** — المصدر في الفوتنوت |
| `estimated` | قيمة + **`basis`** (التقدير جه منين) + **`verifyIn`** (يتراجع كل قد إيه). المصدر اختياري | بادج "تقديري"، والbasis في tooltip |
| `judgment` | قيمة + **`label`** (وصف بالعربي والإنجليزي). ممنوع يكون عليه مصدر | **من غير بادج** — بيتعرض كوصف مش كرقم |
| `NEEDS_VERIFICATION` | `value: null` | بادج "محتاج تأكيد"، ومفيش رقم |

**ليه `estimated` مسموح من غير مصدر:** لو كل تقدير معقول بقى "محتاج تأكيد"، الموقع كله هيبقى بادچات صفرا والبادج يفقد معناه. التقدير مسموح — بس لازم يقول عن نفسه إنه تقدير، ويقول جه منين، وإمتى يتراجع.

**⚠️ `judgment` — أخطر حالة في النظام:**
`arabCommunity` و `gigDemand` **أحكام شخصية مش قياسات.** مفيش مصدر بيرتب المدن رقميًا عليهم.

- **ممنوع تمامًا تعرضها كرقم.** "جالية عربية كبيرة" — **مش** "٤ من ٥". الرقم داخلي للترتيب بس.
- الطريقة الوحيدة لعرض أي حقل هي `display()` في `lib/content/field.ts` — وهي مبترجعش رقم لحقل judgment أصلًا. للترتيب في المحرك استخدم `rankValue()`، والاسم نفسه بيقول إنه للترتيب مش للعرض.
- **في محرك الخطة:** الحقايق بترتب المدن، والأحكام الذاتية بتفصل بين **المتعادلين** بس. `rankWithJudgment()` في `lib/planner/judgment.ts` بيفرض ده، والtests بتتأكد إن حكم ذاتي لوحده ميقدرش يقلب ترشيح مدينة.

## الأوامر
```bash
npm run dev
npm run verify-content   # بيطلع كل حقل NEEDS_VERIFICATION ويكتب TODO-VERIFY.md
npm run test             # unit tests للمحركات الحسابية
npm run build            # بيفشل لو أي record ناقصه sources أو lastVerified
```

## اتفاقيات لازم تتبعها

**الاتجاه (RTL):** استخدم logical properties دايمًا (`padding-inline-start`, `border-inline-start`, `margin-inline-end`). ممنوع `left`/`right`. أي حركة أفقية تاخد الاتجاه من `useDirection()` في `lib/motion.ts`.

**الأرقام:** كلها Latin وLTR وبخط mono. لفّها في `<Num>` component. ممنوع أرقام عربية-هندية.

**النصوص:** ممنوع نص hard-coded في أي component. كله في `messages/ar.json` و `messages/en.json`.

**حالة المستخدم:** مصدر واحد للحقيقة `lib/store/user-store.ts` (zustand + persist). أي أداة بتملّي حقولها من الstore وتقول للمستخدم "ملّينالك ده من بياناتك". المستخدم مايكتبش نفس المعلومة مرتين أبدًا.

**الحركة:** anime.js دايمًا محمّلة · GSAP وframer-motion بـ`dynamic import` بس · عنصر واحد = مكتبة واحدة · كل حركة بتعدي على `useReducedMotion()`.

**الكروت:** كلها `<Card>` من `components/Card.tsx`. متعملش كارت من الأول.

**⚠️ المدينة مش الولاية:** الحاجة للعربية والمواصلات والإيجار كلها بيانات **على مستوى المدينة**، موجودة في `content/metros/`. الولاية فيها الضرايب والرخصة ونظام التأمين بس، في `content/states/`. ممنوع تمامًا تعمّم من الولاية على المدينة — نيويورك ستيت فيها مدينة نيويورك (مش محتاج عربية خالص) وبافالو (محتاج). فرجينيا فيها أرلينجتون (على المترو) وريتشموند (عربية إجبارية). التعميم هنا بيدي نصيحة غلط لحد بيخطط بفلوسه.

## النبرة
عربي مصري عامي، انت لانت، دافي ومباشر. من غير تهويل ومن غير وعود. المستخدم قلقان وبياخد قرار صعب — النبرة دايمًا "دي وضعك، ودي أحسن خطوة جاية"، مش "انت اتأخرت".

## ممنوع
- **تجيب أي ملف أو كود من أي مشروع تاني.** كل حاجة تتكتب من الأول من المواصفات. لو لقيت إشارة لمشروع سابق في أي مكان، دي مواصفات مستخلصة منه — مش دعوة تدوّر عليه ولا تطلب الوصول ليه
- تحط رقم ضريبي أو نسبة أو سعر صرف ثابت في الكود — كله من `content/`
- تخترع أرقام · تدي استشارة قانونية · تضيف backend أو auth أو API keys
- **تعرض حقل `judgment` كرقم** — لا في جدول، لا في شريط، لا في tooltip
- تحمّل GSAP/framer-motion على صفحات المحتوى · تخلي مكتبتين يحركوا نفس العنصر
- تضحي بتباين النص عشان الglass — التباين بيكسب دايمًا
- تخلص مرحلة من غير ما تختبر في اللغتين والثيمين
